// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.2); // Redondea hacia arriba
resultado = Math.floor(2.1); // Redondea hacia abajo
resultado = Math.sqrt(144); // Raiz cuadrada
resultado = Math.abs(-200); // Regresa numero absoluto (200)
resultado = Math.min(1,4,5); // Regresa numero mas chico (1)
resultado = Math.max(1,3,45); // Regresa numero mas alto (45)
resultado = Math.random(); // Regresa numero aleatorio entre 0 y 1
resultado = Math.floor(Math.random() * 30); // Regresa numero aleatorio entre 0 y 30

console.log(resultado);